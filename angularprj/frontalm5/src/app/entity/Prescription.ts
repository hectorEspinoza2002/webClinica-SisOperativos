export class Prescription{

  id?: number;
  details: string = '';
  doctorId?: number;  // Cambiado para coincidir con el backend
  patientId?: number; // Cambiado para coincidir con el backend
  fecha: string = '';

}
